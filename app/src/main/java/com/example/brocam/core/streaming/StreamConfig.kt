package com.example.brocam.core.streaming

import android.util.Size

object StreamConfig {
    const val JPEG_QUALITY = 10
    const val FRAME_RATE_LIMIT_MS = 100 // ~10 FPS
    val RESOLUTION = Size(480, 640)
}
